[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install fastapi==0.104.1 uvicorn==0.24.0 yfinance==0.2.31 pandas==2.1.3 numpy==1.26.2 statsmodels==0.14.0 feedparser==6.0.10 requests==2.31.0"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
